# Valid nixpacks configuration for Railway
# Using generic nodejs package (stable)

[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.install]
cmds = ["chmod +x build.sh && ./build.sh"]

[start]
cmd = "uvicorn src.api.app:app --host 0.0.0.0 --port $PORT"
